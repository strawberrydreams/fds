# 1. Application & Server
spring.application.name=FDS
server.port=8088
server.address=127.0.0.1
server.servlet.context-path=/

# 2. Encoding & Message (UTF-8)
spring.servlet.encoding.charset=UTF-8
spring.servlet.encoding.enabled=true
spring.servlet.encoding.force=true
spring.messages.encoding=UTF-8
spring.thymeleaf.encoding=UTF-8

# 3. View (Thymeleaf)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# 4. Datasource & DB Profile (Oracle)
# 운영 환경에서는 DB_URL / DB_USERNAME / DB_PASSWORD 환경 변수로 오버라이드한다.
spring.profiles.active=ora
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@localhost:1521/FREEPDB1}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.username=${DB_USERNAME:scott}
spring.datasource.password=${DB_PASSWORD:tiger}

# 5. JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.main.allow-bean-definition-overriding=true

# 6. MyBatis Mapping
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.type-aliases-package=kdt.fds.user.dto.response
mybatis.mapper-locations=classpath:mapper/**/*.xml

# 7. External Features & Integrations
# Google Vision OCR 키 파일은 src/main/resources/googlevision.json 위치에 둔다 (GoogleVisionService 참고)
fds.snapshots.base-path=snapshots
spring.messages.basename=messages
spring.devtools.restart.enabled=false
